# 🚀 PLGames Board - Быстрая установка (для клиентов)

**Для тех, кто НЕ имеет опыта с Docker и Linux**

## Требования
- **Linux сервер** (Ubuntu 20.04+, Debian 11+, CentOS 7+) ИЛИ **VPS любого хостера**
- **Docker** + **Docker Compose** (мы установим автоматически)
- **4 ГБ оперативной памяти минимум**
- **20 ГБ свободного места на диске**
- **Интернет соединение**

---

## Шаг 1: Подключитесь к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
# Введите пароль от хоста
```

**Если не знаете IP:** спросите у вашего хостера (Beget, Timeweb, Hetzner, DigitalOcean и т.д.)

---

## Шаг 2: Скопируйте и запустите установщик одной строкой

```bash
curl -sSL https://raw.githubusercontent.com/Leonid1095/PLGames-Board/main/install.sh | bash
```

**Что происходит?**
1. Проверка системы
2. Установка Docker (если не установлен)
3. Клонирование проекта
4. Создание конфигурации
5. Запуск всех сервисов

**Время:** 5-15 минут (зависит от интернета и сервера)

---

## Шаг 3: Ответьте на вопросы (интерактивная установка)

```
Введите домен (или оставьте пусто для localhost): example.com
Введите пароль для базы данных: (придумайте свой)
Настроить firewall? (y/n): y
```

**Советы:**
- Если используете домен, убедитесь что DNS указывает на IP сервера
- Если просто тестируете - оставьте localhost и используйте IP сервера

---

## Шаг 4: Получите URL для доступа

После установки вы увидите:

```
✅ PLGames успешно развернут!

Доступ к приложению:
  Frontend: http://example.com:8080  (или http://ВАШ_IP:8080)
  Backend API: http://example.com:3010
  GraphQL: http://example.com:3010/graphql
```

**Откройте в браузере:** `http://example.com:8080`

---

## Проверка статуса

```bash
cd ~/plgames-board
docker compose ps           # Посмотреть статус контейнеров
docker compose logs -f backend  # Посмотреть логи backend
```

**Все контейнеры должны иметь статус "Running" (зелёный)**

---

## Решение проблем

### Ошибка "Docker not found"
```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose
```

### Ошибка доступа к сервису
```bash
# Проверьте firewall
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 3010/tcp
sudo ufw reload
```

### Сервис не запускается
```bash
cd ~/plgames-board
docker compose restart backend
docker compose logs -n 50 backend  # Посмотрите ошибку
```

### Нужно перезапустить всё
```bash
cd ~/plgames-board
docker compose down
docker compose up -d
```

---

## После установки

1. **Создайте администратора:**
   - Откройте приложение
   - Зарегистрируйтесь с email
   - Вы автоматически админ (первый пользователь)

2. **Пригласите команду:**
   - В приложении есть страница приглашения участников
   - Отправьте ссылки/коды приглашений

3. **Начните работать:**
   - Создавайте проекты, задачи, доски
   - Сотрудничайте с командой в реальном времени

---

## Обновление на новую версию

```bash
cd ~/plgames-board
git pull
docker compose pull
docker compose down && docker compose up -d
```

---

## Поддержка

- **Документация:** https://github.com/Leonid1095/PLGames-Board/blob/main/README.md
- **Issues:** https://github.com/Leonid1095/PLGames-Board/issues
- **Контакт:** Смотрите профиль в GitHub

---

**Готово! Вы просто развернули профессиональное приложение для управления проектами! 🎉**
