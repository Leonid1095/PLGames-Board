# 🤖 PLGames-Board: Внутренний README для ИИ Агента

**Последнее обновление:** 2025-01-XX (Session CRM DI Fixes)  
**Статус:** ✅ Все DI ошибки исправлены, готово к deployment

---

## 🎯 Краткая Версия (30 секунд)

Это проект для управления проектами и задачами (как Jira/Trello).

**Что было сломано:**
- ❌ Backend падал с ошибками DI (Dependency Injection)
- ❌ Клиентов не было инструкций по установке
- ❌ DevOps не было чек-листа для production

**Что мы сделали:**
- ✅ Исправили все 6 DI ошибок в resolvers'ах
- ✅ Создали 5 документов для разных personas
- ✅ Закоммитили все изменения в main

**Текущий статус:** Ожидаем GitHub Actions build нового backend образа, затем production тестирование

---

## 📁 Структура Проекта

```
plgames/
├── packages/
│   ├── backend/        ← NestJS backend (GraphQL/REST API)
│   │   └── server/src/core/
│   │       ├── crm/           ← CRM модуль (проекты, задачи, спринты)
│   │       ├── user/          ← Управление пользователями
│   │       ├── permission/    ← Проверка доступа
│   │       └── ... (otros модули)
│   ├── frontend/       ← Next.js фронтенд
│   └── common/         ← Общий код
├── blocksuite/         ← RichText editor (интеграция)
├── docs/               ← Документация для разработчиков
├── scripts/            ← Утилиты и деплой скрипты
└── tests/              ← E2E тесты
```

**Главные сервисы (docker-compose.yml):**
- `backend` - NestJS API (порт 3000)
- `frontend` - Next.js UI (порт 3000, через Caddy)
- `postgres` - База данных (порт 5432)
- `redis` - Кэш (порт 6379)
- `caddy` - Reverse proxy (порт 80/443)

---

## 🔧 Главные Файлы для Понимания

### Для ИИ Агента (в порядке чтения)

1. **[`ROADMAP.md`](ROADMAP.md)** (5 мин)
   - Текущий статус
   - Что работает, что нет
   - Последние изменения
   - **Зачем:** Обновить себя о состоянии проекта

2. **[`AI_ONBOARDING.md`](AI_ONBOARDING.md)** (10 мин)
   - Карта репозитория и основные папки
   - Все критические проблемы с решениями
   - Правила для разработчиков (очень важно!)
   - Базовый алгоритм для ИИ при кодировании
   - **Зачем:** Узнать, что уже известно и как работать с кодом

3. **[`TECHNICAL_AUDIT.md`](TECHNICAL_AUDIT.md)** (15 мин)
   - Глубокий анализ системы Dependency Injection
   - Таблица всех найденных проблем и решений
   - Почему TypeScript `import type { X }` опасен
   - Чек-лист для audit'а нового кода
   - **Зачем:** Понять, почему было столько ошибок DI

4. **[`SOLUTION_SUMMARY.md`](SOLUTION_SUMMARY.md)** (20 мин)
   - Все найденные проблемы с before/after кодом
   - Хеши коммитов для каждого исправления
   - Для разных сценариев использования
   - **Зачем:** Иметь полный контекст всех изменений

---

## 🐛 Найденные и Исправленные Проблемы

### Session Результаты: 6 DI Ошибок Исправлены

| # | Файл | Проблема | Решение | Коммит |
|---|------|----------|---------|--------|
| 1 | `core/crm/crm.resolver.ts` | CrmIssueResolver: `PrismaService` не существует | → `PrismaClient` | `cf6fa6055` |
| 2 | `core/crm/crm.resolver.ts` | CrmSprintResolver: `PrismaService` не существует | → `PrismaClient` | `402cf3a9e` |
| 3 | `core/crm/crm.resolver.ts` | CrmCommentResolver: `PrismaService` не существует | → `PrismaClient` | `402cf3a9e` |
| 4 | `core/crm/crm.resolver.ts` | CrmTimeLogResolver: `PrismaService` не существует | → `PrismaClient` | `402cf3a9e` |
| 5 | `core/user/resolver.ts` | UserManagementResolver: type-only import | → value import | `d7824ab8a` |
| 6 | `core/user/index.ts` | UserModule не импортирует PrismaModule | → добавили import | `be427c813` |

### Коренные Причины

1. **Type-Only Import Bug**
   - ❌ `import type { PrismaClient } from '@prisma/client'` → TypeScript стирает на compile time
   - ✅ `import { PrismaClient } from '@prisma/client'` → NestJS видит класс
   - Файлы: `user/resolver.ts`, `crm/crm.resolver.ts`

2. **Неправильный Класс**
   - ❌ `PrismaService` (не существует в проекте)
   - ✅ `PrismaClient` (из @prisma/client через PrismaModule)
   - Файлы: все CRM resolver'ы

3. **Missing Module Import**
   - ❌ UserModule не импортирует PrismaModule
   - ✅ Добавили `imports: [PrismaModule]` в UserModule
   - Файл: `core/user/index.ts`

---

## 🛡️ Правила для Разработки (ОБЯЗАТЕЛЬНО!)

### Правило 1: Никогда Не Используй `import type` для DI

```typescript
// ❌ НЕПРАВИЛЬНО - NestJS не сможет инжектировать
import type { PrismaClient } from '@prisma/client';

@Injectable()
export class MyService {
  constructor(private prisma: PrismaClient) {} // ошибка!
}

// ✅ ПРАВИЛЬНО - NestJS сможет инжектировать
import { PrismaClient } from '@prisma/client';

@Injectable()
export class MyService {
  constructor(private prisma: PrismaClient) {} // работает!
}
```

### Правило 2: Global Модули Требуют Явного Импорта

```typescript
// ❌ НЕПРАВИЛЬНО - даже если PrismaModule @Global()
@Module({
  controllers: [UserController],
  providers: [UserService],
})
export class UserModule {}

// ✅ ПРАВИЛЬНО - всегда импортируй @Global() модули явно
@Module({
  imports: [PrismaModule], // даже если @Global()!
  controllers: [UserController],
  providers: [UserService],
})
export class UserModule {}
```

### Правило 3: Всегда Проверяй Существование Класса

```typescript
// ❌ НЕПРАВИЛЬНО - PrismaService не существует в проекте
import { PrismaService } from '@/core/lib/prisma';

// ✅ ПРАВИЛЬНО - используй PrismaClient из Prisma пакета
import { PrismaClient } from '@prisma/client';
```

### Правило 4: Алгоритм Отладки DI Ошибок

Если видишь `UnknownDependenciesException`:

1. **Найди resolver/service** с ошибкой (из error message)
2. **Проверь все import'ы** - нет ли `import type`?
3. **Проверь module imports** - импортирует ли модуль зависимый модуль?
4. **Проверь существование класса** - класс реально создан и экспортирован?
5. **Проверь provider** - класс добавлен в `providers: []`?
6. **Проверь export** - модуль экспортирует нужный класс?

---

## 📚 Что Нужно Читать Если...

### Если добавляешь новый resolver/service с DI
1. Прочитай **Правило 1-3** выше
2. Посмотри на существующий resolver как пример
3. Используй `import { Class } from '@/path'` (БЕЗ `type`)

### Если видишь DI ошибку
1. Применяй **Правило 4: Алгоритм Отладки**
2. Смотри раздел "Таблица Ошибок" в `TECHNICAL_AUDIT.md`

### Если нужно исправить deploy
1. Читай `DEPLOYMENT_CHECKLIST.md`
2. Понимай: new code → GitHub push → Actions build (5-10 мин) → Pull на server

### Если нужно помочь клиенту с установкой
1. Дай им команду из `INSTALLATION_QUICK.md`
2. Если проблема - скажи открыть GitHub Issue с логами

---

## 🚀 Быстрый Старт для Разработки

### Локальная Установка

```bash
# Клонируй репо
git clone https://github.com/Leonid1095/PLGames-Board.git
cd plgames-board

# Запусти через Docker
docker compose up

# Или локально (нужны Node 18+, pnpm, postgres, redis)
cd plgames
pnpm install
pnpm build
pnpm start
```

### Основные Команды

```bash
# Смотри логи backend
docker compose logs -f backend

# Перезапусти backend
docker compose restart backend

# Заходи в базу данных
docker exec -it plgames-board-postgres-1 psql -U plgames

# Проверь статус сервисов
docker compose ps
```

---

## 🔍 Как Найти Код

### Найти resolver для CRM проектов
```bash
# Файл с resolvers'ами
plgames/packages/backend/server/src/core/crm/crm.resolver.ts

# Сервис для бизнес-логики
plgames/packages/backend/server/src/core/crm/crm.service.ts

# Модуль с конфигурацией
plgames/packages/backend/server/src/core/crm/crm.module.ts
```

### Найти что-то конкретное
```bash
# Поиск по коду
grep -r "ClassName" plgames/packages/backend/server/src/

# Поиск импортов
grep -r "import.*ClassName" plgames/
```

---

## 📊 Текущий Статус Дэплоймента

### Что Ожидается

1. **GitHub Actions** 
   - Статус: Должен запуститься при push в main (commit 3b36f8351)
   - Что делает: Собирает backend Docker образ, публикует в ghcr.io
   - Время: 5-10 минут
   - Образ: `ghcr.io/leonid1095/plgames-board-backend:main-3b36f8351`

2. **Production Server**
   - Команда для обновления: `cd ~/plgames-board; git pull; docker compose pull; docker compose up -d`
   - Ожидаемый результат: Backend запустится без DI ошибок
   - Проверка: `docker compose logs -n 100 backend | grep "successfully started"`

3. **Валидация**
   - ✅ Нет UnknownDependenciesException
   - ✅ Нет Cannot instantiate...
   - ✅ GraphQL schema загружается
   - ✅ API отвечает на http://domain.com/graphql

---

## 🎓 Обучающие Материалы

### Если незнаком с NestJS DI
- https://docs.nestjs.com/fundamentals/custom-providers
- https://docs.nestjs.com/fundamentals/modules
- https://docs.nestjs.com/fundamentals/circular-dependency

### Если незнаком с TypeScript
- https://www.typescriptlang.org/docs/handbook/
- Ключевая тема: `type` vs `interface` vs value imports

### Если незнаком с Docker/Compose
- https://docs.docker.com/compose/
- Важно: order of services, health checks, volume management

---

## 📞 Как Внести Свой Вклад

### Если нашел баг DI
1. Прочитай `TECHNICAL_AUDIT.md` - может уже известен
2. Если новый - открой Issue с полным stacktrace
3. Примени **Правило 4: Алгоритм Отладки** при фиксе

### Если добавляешь новый код
1. Прочитай `AI_ONBOARDING.md` раздел "Правила для разработчиков"
2. Смотри на existing code как примеры
3. Перед commit'ом проверь: нет ли `import type` для DI?

### Если улучшаешь документацию
1. Используй этот файл как шаблон структуры
2. Обновляй ROADMAP.md с изменениями
3. Commit сообщение: `docs: describe what changed`

---

## ⚠️ Частые Проблемы и Решения

### Проблема: "UnknownDependenciesException"
**Решение:** Читай **Правило 4: Алгоритм Отладки**

### Проблема: "Cannot instantiate abstract class"
**Решение:** Класс помечен как `@Injectable()` но НЕ добавлен в `providers: []` модуля

### Проблема: Docker образ не обновляется
**Решение:** 
1. Проверь `docker compose pull`
2. Дай GitHub Actions 5-10 минут на build
3. Используй явный тег вместо `latest`: `ghcr.io/.../backend:main-<commit>`

### Проблема: Backend не загружает GraphQL schema
**Решение:** Проверь логи: `docker compose logs backend`
Обычно это DI ошибка

---

## 🎯 Что Делать Дальше (TODO для следующего сеанса)

### Приоритет 1: Валидация на Production ✓ (PENDING)
- [ ] Дождись GitHub Actions build'а
- [ ] Выполни на production: `docker compose pull && docker compose up -d --force-recreate backend`
- [ ] Проверь логи: `docker compose logs -n 100 backend`
- [ ] Подтверди: нет "UnknownDependenciesException"

### Приоритет 2: Автоматизация (НЕ НАЧАТО)
- [ ] Создай `scripts/validate-di.js` для проверки DI на build-time
- [ ] Интегрируй в `yarn build` pipeline
- [ ] Добавь в GitHub Actions (fail build если DI ошибки)

### Приоритет 3: Мониторинг (НЕ НАЧАТО)
- [ ] Добавь health check endpoint
- [ ] Интегрируй мониторинг (Prometheus + Grafana)
- [ ] Настрой алерты на DI ошибки

---

## 📝 История Сессии

**Дата:** 2025-01-XX  
**Инициатор:** User (Русский)  
**Задача:** "Проанализируй весь проект, почему не получается развернуть на сервере?"

**Что сделано:**
1. ✅ Проанализировал 6 файлов backend code
2. ✅ Нашел 6 DI ошибок в resolvers'ах
3. ✅ Исправил все ошибки (4 commits)
4. ✅ Создал 5 документов (1800+ строк)
5. ✅ Закоммитил всё в main

**Коммиты:**
- `cf6fa6055` - Fix CrmIssueResolver DI
- `402cf3a9e` - Fix 3 CRM resolvers DI
- `d7824ab8a` - Fix UserManagementResolver import
- `be427c813` - Fix UserModule PrismaModule import
- `918594cdd` - Add AI_ONBOARDING and INSTALLATION_QUICK
- `a7a02fdf1` - Add DEPLOYMENT_CHECKLIST
- `cce863759` - Add SOLUTION_SUMMARY
- `3b36f8351` - Add DOCS_GUIDE (this session)

---

**Следующий ИИ Агент:** Начни с раздела "Краткая Версия", затем читай документы в порядке выше! 🚀
