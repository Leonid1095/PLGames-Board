# PLGames Board - Быстрый старт

## 🚀 Самый простой способ (рекомендуется для продакшн)

Всего **одна команда** - скрипт всё сделает сам:

```bash
curl -fsSL https://raw.githubusercontent.com/Leonid1095/boards_plane/main/install-auto.sh | sudo bash
```

### Что произойдет:

1. ✅ Проверит систему (RAM, диск, swap)
2. ✅ Создаст swap 4GB (если нужно)
3. ✅ Установит Docker и Docker Compose
4. ✅ Установит Node.js 22
5. ✅ Склонирует проект
6. ✅ Спросит: домен с HTTPS или IP с HTTP
7. ✅ Соберет проект (20-30 минут)
8. ✅ Запустит все сервисы
9. ✅ Покажет URL для доступа

### Требования:

- **8GB RAM** (скрипт создаст 4GB swap автоматически)
- **20GB свободного места** на диске
- **Ubuntu 20.04+** или **Debian 11+**
- **Root доступ** (sudo)

### Время установки:

- Первый раз: **25-35 минут** (сборка)
- Повторный запуск: **2-3 минуты** (использует готовую сборку)

---

## 📱 Что дальше?

После установки откройте URL из сообщения в браузере:
- Для HTTPS: `https://ваш-домен.com`
- Для HTTP: `http://ваш-ip:8080`

Создайте первый аккаунт - он станет администратором.

---

## 🛠️ Управление

```bash
cd ~/plane

# Статус
docker compose -f docker-compose.simple.yml ps

# Логи
docker compose -f docker-compose.simple.yml logs -f

# Перезапуск
docker compose -f docker-compose.simple.yml restart

# Остановка
docker compose -f docker-compose.simple.yml down
```

---

## 📚 Подробная документация

См. [INSTALL.md](INSTALL.md) для других способов установки.
