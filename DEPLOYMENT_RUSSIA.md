# PLGames Board - Развертывание в России

**Проблема:** Сборка Docker образов в России не работает из-за блокировки `binaries.prisma.sh` (Prisma engines) и нестабильного доступа к `sh.rustup.rs`.

**Решение:** Использовать **готовые Docker образы**, собранные на GitHub Actions (не в России).

---

## 🚀 Быстрая установка (Рекомендуется для России)

### Вариант 1: Через install.sh с готовыми образами

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Leonid1095/PLGames-Board/main/install.sh)
```

Скрипт автоматически:
1. Клонирует репозиторий
2. Создаст конфигурацию (.env)
3. **Скачает готовые Docker образы** из GitHub Container Registry
4. Запустит все сервисы

**Время:** 5-10 минут (вместо 30 минут сборки)

---

### Вариант 2: Ручная установка с готовыми образами

```bash
# 1. Клонировать репозиторий
git clone https://github.com/Leonid1095/PLGames-Board.git ~/plgames-board
cd ~/plgames-board

# 2. Создать .env файл
cp .env.example .env
nano .env  # Укажите домен и пароль БД

# 3. Скачать готовые образы (НЕ собирать локально)
docker pull ghcr.io/leonid1095/plgames-board-backend:latest
docker pull ghcr.io/leonid1095/plgames-board-frontend:latest

# 4. Запустить (БЕЗ --build флага!)
docker compose up -d
```

---

## 📦 Готовые Docker образы

Образы автоматически собираются на GitHub Actions при каждом push в `main`:

| Образ | Ссылка | Размер |
|-------|--------|--------|
| Backend | `ghcr.io/leonid1095/plgames-board-backend:latest` | ~800 MB |
| Frontend | `ghcr.io/leonid1095/plgames-board-frontend:latest` | ~50 MB |

### Как это работает:

1. **GitHub Actions** собирает образы на серверах GitHub (США/Европа) - там нет блокировок
2. Готовые образы пушатся в **GitHub Container Registry** (ghcr.io)
3. Вы просто **скачиваете** готовые образы на свой сервер в России

---

## 🔄 Обновление до новой версии

```bash
cd ~/plgames-board

# Остановить контейнеры
docker compose down

# Обновить код
git pull origin main

# Скачать новые образы
docker compose pull

# Запустить обновленные контейнеры
docker compose up -d
```

**Данные сохраняются** в Docker volumes (postgres_data, redis_data, storage_data).

---

## 🛠️ Локальная сборка (❌ НЕ работает в России)

**ВАЖНО:** Локальная сборка **НЕ РАБОТАЕТ** в России из-за блокировки `binaries.prisma.sh`.

Prisma engines (критичный компонент) **не имеют зеркал**, в отличие от Rust/NPM. Это означает:

- ❌ Сборка **всегда** упадёт на этапе `prisma generate`
- ❌ Даже с VPN результат **не гарантирован** (нестабильное соединение)
- ⏱️ Сборка занимает 25-30 минут до неизбежной ошибки

**Решение:** Используйте готовые образы (см. выше) ⬆️

---

## 🌐 Автоматическая сборка образов

Образы собираются автоматически при:
- Push в ветку `main`
- Изменении файлов в `plgames/`, `packages/` или `docker-compose.yml`
- Ручном запуске workflow

### Статус сборки:

[![Build Docker Images](https://github.com/Leonid1095/PLGames-Board/actions/workflows/build-docker.yml/badge.svg)](https://github.com/Leonid1095/PLGames-Board/actions/workflows/build-docker.yml)

### Посмотреть логи сборки:

https://github.com/Leonid1095/PLGames-Board/actions

---

## 🔍 Проверка образов

```bash
# Посмотреть скачанные образы
docker images | grep plgames-board

# Ожидаемый вывод:
# ghcr.io/leonid1095/plgames-board-backend   latest   ...   800MB
# ghcr.io/leonid1095/plgames-board-frontend  latest   ...   50MB

# Проверить из какого источника образ
docker inspect ghcr.io/leonid1095/plgames-board-backend:latest | grep Created
```

---

## ❓ FAQ

### Почему нельзя собрать в России?

**Prisma engines** (библиотеки для работы с БД) скачиваются только с `binaries.prisma.sh`, который **заблокирован в России**. К сожалению, **зеркал нет** (в отличие от Rust/NPM).

### Безопасны ли готовые образы?

✅ **Да**. Образы собираются на GitHub Actions из **открытого исходного кода** этого репозитория. Вы можете:
- Посмотреть [workflow файл](.github/workflows/build-docker.yml)
- Проверить [логи сборки](https://github.com/Leonid1095/PLGames-Board/actions)
- Пересобрать на своём сервере вне России

### Как часто обновляются образы?

Автоматически при каждом push в `main`. Обычно несколько раз в неделю.

### Можно ли использовать другой registry?

Да, можете пересобрать и запушить в свой Docker Hub или private registry:

```bash
# Собрать на сервере ВНЕ России
git clone https://github.com/Leonid1095/PLGames-Board.git
cd PLGames-Board

# Собрать и запушить в свой registry
docker build -t your-registry.com/plgames-backend:latest --target backend -f plgames/Dockerfile plgames/
docker push your-registry.com/plgames-backend:latest

# На сервере в России
docker pull your-registry.com/plgames-backend:latest
```

---

## 📊 Сравнение методов установки

| Метод | Время | Требования | Работает в России |
|-------|-------|------------|-------------------|
| **Готовые образы (рекомендуется)** | 5-10 мин | Docker | ✅ Да |
| Локальная сборка | ❌ Не работает | Docker + 8GB RAM + VPN | ❌ Нет (Prisma заблокирован) |
| Сборка на GitHub Actions | 15-20 мин | Форк репозитория | ✅ Да (автоматически) |

---

## 🎯 Следующие шаги

После установки:

1. **Откройте в браузере**: `http://ваш-домен` (или `http://IP-адрес`)
2. **Зарегистрируйте первого пользователя** - он станет администратором
3. **Настройте firewall** (порты 80/443)
4. **Настройте DNS** (если используете домен)

---

**Вопросы?** Создайте [issue на GitHub](https://github.com/Leonid1095/PLGames-Board/issues)

**PLGames Board** - независимая система управления проектами! 🚀
