# Quick Installation Test - PLGames Board

**Быстрый тест установки (от лица клиента)**

---

## Вариант 1: Полностью автоматическая установка (Рекомендуется)

На **чистом сервере** с Docker выполните одну команду:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Leonid1095/PLGames-Board/main/install.sh)
```

### Что будет спрошено:

1. **Домен** - просто нажмите Enter для `localhost`
2. **Порты** - просто нажмите Enter (80/443 по умолчанию)
3. **Firewall** - введите `n` для теста на localhost
4. **Подтверждение** - введите `y`

### Что произойдет дальше (автоматически):

```
📦 Клонирование репозитория...
   → git clone в ~/plgames-board

⚙️  Создание конфигурации...
   → .env файл с автоматическим паролем БД

📥 Скачивание готовых Docker образов (5-10 минут)...
   → Backend image (~800 MB) из ghcr.io
   → Frontend image (~50 MB) из ghcr.io
   → PostgreSQL, Redis, Caddy из Docker Hub

🚀 Запуск сервисов...
   → Создание Docker volumes
   → Старт всех контейнеров

⏳ Ожидание запуска сервисов (1-2 минуты)...
   → PostgreSQL healthcheck
   → Backend healthcheck
   → Redis запуск

✅ Установка завершена!
   → URL: http://localhost
   → Первый пользователь = админ
```

**⚡ Важно:** Установка теперь **НЕ собирает** образы локально! Вместо этого скачиваются готовые образы, собранные на GitHub Actions. Это работает в России без VPN.

---

## Вариант 2: Ручная установка (для разработчиков)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/Leonid1095/PLGames-Board.git ~/plgames-board
cd ~/plgames-board

# 2. Запустить интерактивный установщик
bash install.sh
```

Дальше так же как в Варианте 1.

---

## После установки

### 1. Проверить что всё работает:

```bash
cd ~/plgames-board

# Статус сервисов (все должны быть "Up")
docker compose ps

# Логи backend
docker compose logs backend --tail=50

# Health check
curl http://localhost:3010/api/healthz
```

**Ожидаемый вывод docker compose ps:**
```
NAME                     STATUS          PORTS
plgames-backend          Up (healthy)    0.0.0.0:3010->3010/tcp
plgames-frontend         Up
plgames-gateway          Up              0.0.0.0:80->80/tcp, 0.0.0.0:443->443/tcp
plgames-postgres         Up (healthy)
plgames-redis            Up (healthy)
```

### 2. Открыть в браузере:

```
http://localhost
```

**Ожидаемая страница:** Регистрация / Вход в PLGames Board

### 3. Зарегистрировать первого пользователя:

- Email: `admin@example.com`
- Пароль: любой безопасный пароль

**⚠️ ВАЖНО:** Первый зарегистрированный пользователь автоматически становится администратором!

---

## Обновление установки

```bash
# Способ 1: Через установщик (обновляет код и образы)
bash <(curl -fsSL https://raw.githubusercontent.com/Leonid1095/PLGames-Board/main/install.sh)
# Выберите: Remove and reinstall? → n (сохранит данные)

# Способ 2: Вручную (быстрее)
cd ~/plgames-board
docker compose down           # Остановить контейнеры
git pull origin main          # Обновить код
docker compose pull           # Скачать новые образы
docker compose up -d          # Запустить обновленные контейнеры
```

**Данные сохраняются** в Docker volumes (postgres_data, redis_data, storage_data).

**⚡ Важно:** Обновление теперь **НЕ пересобирает** образы (нет `--build`)! Вместо этого скачиваются новые готовые образы с GitHub.

---

## Полезные команды

```bash
cd ~/plgames-board

# Просмотр логов всех сервисов
docker compose logs -f

# Просмотр логов конкретного сервиса
docker compose logs backend -f
docker compose logs frontend -f
docker compose logs postgres -f

# Перезапуск всех сервисов
docker compose restart

# Перезапуск конкретного сервиса
docker compose restart backend

# Остановка всех сервисов
docker compose down

# Запуск всех сервисов
docker compose up -d

# Пересборка и запуск (после изменения кода)
docker compose up -d --build

# Посмотреть использование ресурсов
docker stats

# Подключиться к базе данных
docker compose exec postgres psql -U plgames -d plgames
```

---

## Устранение проблем

### Проблема: Порты 80/443 заняты

**Симптом:**
```
Error: failed to start containers: gateway
bind: address already in use
```

**Решение 1:** Остановить конфликтующий сервис
```bash
sudo systemctl stop caddy
# или
sudo systemctl stop nginx
```

**Решение 2:** Использовать другие порты при установке
```
HTTP port: 8080
HTTPS port: 8443
```

Тогда доступ будет по `http://localhost:8080`

### Проблема: Сборка падает с ошибкой Rust

**Симптом:**
```
Failed to download from https://rsproxy.cn/rustup-init.sh
Failed to download from https://sh.rustup.rs
Failed to download from https://mirrors.tuna.tsinghua.edu.cn/...
ERROR: Failed to install Rust from all mirrors
```

**Решение:**
1. Проверьте интернет-соединение
2. Попробуйте запустить установку еще раз (может быть временная недоступность)
3. Если в России - проверьте доступность зеркал:
   ```bash
   curl -I https://rsproxy.cn/rustup-init.sh
   curl -I https://mirrors.tuna.tsinghua.edu.cn/rustup/rustup-init.sh
   ```

### Проблема: Backend не стартует

**Симптом:**
```
backend    | Error: Cannot find package '@affine/server-native'
```

**Решение:**
```bash
cd ~/plgames-board
docker compose down
docker compose build --no-cache backend
docker compose up -d
```

### Проблема: "Out of memory" во время сборки

**Симптом:**
```
FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory
```

**Решение:**
1. Закрыть другие приложения
2. Добавить swap (временно):
   ```bash
   sudo fallocate -l 4G /swapfile
   sudo chmod 600 /swapfile
   sudo mkswap /swapfile
   sudo swapon /swapfile
   ```
3. Использовать сервер с 8GB+ RAM

---

## Тест для России

Если сервер находится в России, установка **работает без VPN**:

```bash
cd ~/plgames-board

# Проверить что образы скачаны с GitHub Container Registry
docker images | grep plgames-board

# Ожидаемый вывод:
# ghcr.io/leonid1095/plgames-board-backend   latest   ...   800MB
# ghcr.io/leonid1095/plgames-board-frontend  latest   ...   50MB
```

**Если образы есть - всё работает правильно! 🎉**

**Почему это работает в России:**
- ✅ Образы собраны на GitHub Actions (США/Европа, нет блокировок)
- ✅ Образы хранятся на ghcr.io (доступен в России)
- ✅ Локальная сборка **не требуется** (обходим блокировку Prisma)
- ✅ Обновление через `docker compose pull` (без пересборки)

---

## Бэкап данных

```bash
cd ~/plgames-board

# Создать backup базы данных
docker compose exec postgres pg_dump -U plgames plgames > backup_$(date +%Y%m%d).sql

# Создать backup файлов пользователей
docker run --rm -v plgames-board_storage_data:/data -v $(pwd):/backup ubuntu tar czf /backup/storage_backup_$(date +%Y%m%d).tar.gz /data
```

---

## Полное удаление

```bash
# Остановить и удалить контейнеры
cd ~/plgames-board
docker compose down -v  # -v удаляет volumes (ВСЕ ДАННЫЕ!)

# Удалить директорию
cd ~
rm -rf ~/plgames-board

# Удалить образы (опционально)
docker image prune -a
```

**⚠️ ВНИМАНИЕ:** Это удалит ВСЕ данные пользователей, документы, БД!

---

**Версия:** 2024-12-14
**Тестировано на:** Ubuntu 22.04, Debian 12
**Среднее время установки:** 25-30 минут (с учётом сборки)
